enum AccountType { user, rider }

enum ResponseStatus { success, failed }

enum Status { pending, accepted, arrived, riding, completed }

enum Mode { loading, normal }

enum LocationType { pickup, destination }

enum AppMode { rider, user }
